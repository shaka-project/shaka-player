name: Lint Shaka Player

# TODO: description

runs:
  using: composite # TODO: what does this mean?
  steps:
    - name: Lint
      shell: bash
      run: |
        # TODO: echo markers?
        cd src/
        build/check.py